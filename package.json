{
  "name": "fabricum",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "test": "node --test --test-isolation=none front-end/tests/*.test.js scripts/**/*.test.cjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "native:install": "node scripts/install-native-codecs.mjs",
    "release": "bash scripts/release/release.sh",
    "release:archive": "node scripts/release.mjs",
    "release:binary": "node scripts/release/binary.mjs",
    "scripts:test": "node --test --test-isolation=none scripts/**/*.test.cjs"
  },
  "description": "A game asset processing and optimization toolkit"
}
